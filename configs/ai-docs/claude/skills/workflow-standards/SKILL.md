---
description: "Workflow guidelines for plan-driven implementation. USE PROACTIVELY when planning implementation for a non-trivial task or starting a new feature."
user-invocable: false
---

# Workflow Standards -- Examples & Patterns

Reference examples for the WORKFLOW rules defined in CLAUDE.md.

---

## Test title design + RED-GREEN-REFACTOR cycle

For plan-driven implementation, the canonical TDD/BDD discipline lives in the `test-driven-development` skill: title design before implementation, RED-GREEN-REFACTOR most-forcing-case-first, helper-on-demand, rationalization tables for drift, and the `manual-tests-evidences.md` format. Load it when starting any plan task.
